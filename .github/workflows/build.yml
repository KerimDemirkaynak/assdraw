name: Build Windows Only

on:
  push:
    branches: [ master, main ]
  pull_request:
    branches: [ master, main ]

jobs:
  build-windows:
    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v4

    - uses: actions/setup-python@v5
      with:
        python-version: '3.x'

    - name: Setup Meson & Ninja
      run: pip install meson ninja

    - name: Setup MSVC
      uses: ilammy/msvc-dev-cmd@v1

    - name: Install Dependencies (Windows)
      shell: cmd
      run: |
        echo "Inno Setup Kuruluyor..."
        choco install innosetup
        
        echo "vcpkg.json varsa siliniyor..."
        if exist vcpkg.json del vcpkg.json

        echo "wxWidgets vcpkg ile kuruluyor..."
        cd /D C:\vcpkg
        git pull
        call bootstrap-vcpkg.bat
        REM Sadece wxwidgets kuruyoruz, AGG'yi manuel indirecegiz.
        vcpkg install wxwidgets --triplet x64-windows

    - name: Download and Extract AGG
      shell: powershell
      run: |
        echo "AGG 2.5 indiriliyor..."
        Invoke-WebRequest -Uri "https://sourceforge.net/projects/agg/files/agg/2.5/agg-2.5.zip/download" -OutFile "agg.zip" -UserAgent "NativeHost"
        echo "Zipten cikariliyor..."
        Expand-Archive agg.zip -DestinationPath . -Force
        # Klasor adi 'agg-2.5' olacak

    - name: Configure Meson
      shell: cmd
      run: |
        REM vcpkg kutuphanelerini Meson'a tanitiyoruz
        meson setup build --buildtype=release --backend=ninja -Dcmake_prefix_path=C:/vcpkg/installed/x64-windows

    - name: Build
      run: meson compile -C build

    - name: Prepare for Installer
      shell: cmd
      run: |
        if not exist build\Release mkdir build\Release
        copy build\ASSDraw3.exe build\Release\ASSDraw3.exe

    - name: Generate Installer
      shell: cmd
      run: |
        "C:\Program Files (x86)\Inno Setup 6\ISCC.exe" installer.iss

    - name: Upload Artifacts
      uses: actions/upload-artifact@v4
      with:
        name: ASSDraw3-Windows
        path: |
          build/ASSDraw3.exe
          Output/ASSDraw3_Setup.exe
